# Attachments

Images and other files dropped into notes land here. Settings → Files and links → Default location for new attachments is `π-imgs`.

The `π-` prefix puts this folder at the bottom of the explorer: it is storage, not a working surface. See [[dev/style-guide/folder-prefixes]].
